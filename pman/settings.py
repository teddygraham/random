# settings.py

# Define some colors
BLACK = (0, 0, 0)
WHITE = (255, 255, 255)
YELLOW = (255, 255, 0)
BLUE = (0, 0, 255)
RED = (255, 0, 0)

# Game window settings
TILE_SIZE = 50
WINDOW_WIDTH = 550  # Adjusted according to the map size
WINDOW_HEIGHT = 600  # Adjusted according to the map size
WINDOW_SIZE = (WINDOW_WIDTH, WINDOW_HEIGHT)

# Player settings
PLAYER_LIVES = 3

# Timer settings
TIME_LIMIT = 60
